[
  {
    "name": "Check payment status via menu button",
    "steps": [
      {
        "send": { "callback": "payment:check" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "editMessageText",
            "payload": {
              "text": "Enter your M-Pesa transaction ID or phone number to check payment status:"
            }
          }
        ]
      }
    ]
  }
]
